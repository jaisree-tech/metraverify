# Locally, you can leave this file out entirely (defaults to http://localhost:8000).
# When deploying the frontend (e.g. Render Static Site), create a real .env with:
VITE_API_BASE_URL=http://localhost:8000
